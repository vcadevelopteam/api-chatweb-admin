pipeline {
    agent any

    tools {
        nodejs '14.21.3'
    }

    stages {
        stage('Build') {
            steps {
                sh 'cd /var/www/html/api-laraigo-dev && sudo git checkout testing && sudo git pull && sudo npm install'
            }
        }
        stage('Deploy') {
            steps {
                sh 'sudo pm2 restart 1'
                //comment test
            }
        }
    }
}
