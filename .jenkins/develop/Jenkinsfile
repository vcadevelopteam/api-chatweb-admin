pipeline {
    agent any

    tools {
        nodejs '14.21.3'
    }

    stages {
        stage('Build') {
            steps {
                sh 'cd /var/www/html/api-laraigo-dev'
                sh 'sudo git checkout testing'
                sh 'sudo git pull'
            }
        }
        stage('Deploy') {
            steps {
                sh 'cd /var/www/html/api-laraigo-dev'
                sh 'npm install'
                sh 'sudo pm2 restart 1'
            }
        }
    }
}
