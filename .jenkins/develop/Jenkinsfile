pipeline {
    agent any

    environment {
        TMPDIR = "/var/www/html/api-laraigo-test"
    }

    tools {
        nodejs '14.21.3'
    }

    stages {
        stage('Deploy') {
            steps {
                sh 'pm2 list'
            }
        }
    }
}
