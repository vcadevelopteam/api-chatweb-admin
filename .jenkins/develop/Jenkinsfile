pipeline {
    agent any

    tools {
        nodejs '14.21.3'
    }

    stages {
        stage('Deploy') {
            steps {
                script {
                    sshagent(credentials: ['root-ssh-key']) {
                        sh """
                            cd /var/www/html/api-laraigo-dev
                            whoami
                            pwd
                            pm2 list
                        """
                    }
                }
            }
        }
    }
}
