pipeline {
    agent any

    tools {
        nodejs '14.21.3'
    }

    stages {
        stage('Build') {
            steps {
                sh 'cd /var/www/html/api-laraigo-test'
                sh 'sudo git checkout testing'
                sh 'sudo git pull'
            }
        }
        stage('Deploy') {
            steps {
                sh 'cd /var/www/html/api-laraigo-test'
                sh 'npm install'
                sh 'sudo pm2 restart 2'
            }
        }
    }
}
